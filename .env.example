# ChatFilter Configuration Example
# Пример конфигурации ChatFilter
#
# Copy this file to .env and customize for your environment
# Скопируйте этот файл в .env и настройте под вашу среду
#
# Configuration priority / Приоритет конфигурации:
# 1. CLI arguments (highest) / Аргументы командной строки (наивысший)
# 2. Environment variables / Переменные окружения
# 3. .env file (this file) / Файл .env (этот файл)
# 4. Default values (lowest) / Значения по умолчанию (наименьший)

# ==============================================================================
# SERVER SETTINGS / НАСТРОЙКИ СЕРВЕРА
# ==============================================================================

# Server host to bind to
# Хост сервера для привязки
# Default: 127.0.0.1 (localhost only)
# Use 0.0.0.0 to allow external connections / Используйте 0.0.0.0 для внешних подключений
CHATFILTER_HOST=127.0.0.1

# Server port to bind to
# Порт сервера для привязки
# Default: 8000
# Valid range: 1-65535
CHATFILTER_PORT=8000

# Enable debug mode (verbose logging, auto-reload)
# Включить режим отладки (подробное логирование, авто-перезагрузка)
# Default: false
# Options: true, false
CHATFILTER_DEBUG=false

# ==============================================================================
# PATHS / ПУТИ
# ==============================================================================

# Base directory for application data
# Базовая директория для данных приложения
# Default: Platform-specific (see below) / По умолчанию: зависит от платформы
#   macOS: ~/Library/Application Support/ChatFilter
#   Linux: ~/.local/share/chatfilter
#   Windows: %APPDATA%/ChatFilter
# Uncomment to override / Раскомментируйте для переопределения:
# CHATFILTER_DATA_DIR=/path/to/data

# ==============================================================================
# LOGGING / ЛОГИРОВАНИЕ
# ==============================================================================

# Logging level
# Уровень логирования
# Default: INFO
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
CHATFILTER_LOG_LEVEL=INFO

# Enable logging to file (in addition to console)
# Включить логирование в файл (в дополнение к консоли)
# Default: true
# Options: true, false
CHATFILTER_LOG_TO_FILE=true

# Maximum size of each log file before rotation (in bytes)
# Максимальный размер каждого лог-файла до ротации (в байтах)
# Default: 10485760 (10 MB)
# Valid range: 1048576-104857600 (1 MB - 100 MB)
CHATFILTER_LOG_FILE_MAX_BYTES=10485760

# Number of backup log files to keep
# Количество резервных лог-файлов для хранения
# Default: 5
# Valid range: 1-20
CHATFILTER_LOG_FILE_BACKUP_COUNT=5

# ==============================================================================
# CORS SETTINGS / НАСТРОЙКИ CORS
# ==============================================================================

# Allowed CORS origins (comma-separated)
# Разрешенные CORS источники (через запятую)
# Default: localhost origins for common development ports
# По умолчанию: localhost источники для распространённых портов разработки
# Production example / Пример для продакшена:
# CHATFILTER_CORS_ORIGINS=https://example.com,https://app.example.com
CHATFILTER_CORS_ORIGINS=http://localhost:8000,http://127.0.0.1:8000,http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173,http://127.0.0.1:5173,http://localhost:4200,http://127.0.0.1:4200

# ==============================================================================
# TELEGRAM SETTINGS / НАСТРОЙКИ TELEGRAM
# ==============================================================================

# Maximum number of messages to fetch per chat
# Максимальное количество сообщений для загрузки из чата
# Default: 10000
# Valid range: 100-100000
CHATFILTER_MAX_MESSAGES_LIMIT=10000

# ==============================================================================
# CONNECTION TIMEOUTS / ТАЙМАУТЫ ПОДКЛЮЧЕНИЯ
# ==============================================================================

# Timeout for Telegram connection (in seconds)
# Таймаут для подключения к Telegram (в секундах)
# Default: 30.0
# Valid range: 5.0-300.0
CHATFILTER_CONNECT_TIMEOUT=30.0

# Timeout for Telegram operations (in seconds)
# Таймаут для операций Telegram (в секундах)
# Default: 60.0
# Valid range: 10.0-600.0
CHATFILTER_OPERATION_TIMEOUT=60.0

# ==============================================================================
# CONNECTION HEALTH MONITORING / МОНИТОРИНГ ЗДОРОВЬЯ ПОДКЛЮЧЕНИЯ
# ==============================================================================

# Interval between heartbeat checks (in seconds)
# Интервал между проверками heartbeat (в секундах)
# Default: 60.0
# Valid range: 10.0-600.0
CHATFILTER_HEARTBEAT_INTERVAL=60.0

# Timeout for heartbeat ping operations (in seconds)
# Таймаут для операций ping heartbeat (в секундах)
# Default: 10.0
# Valid range: 5.0-60.0
CHATFILTER_HEARTBEAT_TIMEOUT=10.0

# Maximum consecutive heartbeat failures before reconnection
# Максимальное количество последовательных сбоев heartbeat до переподключения
# Default: 3
# Valid range: 1-10
CHATFILTER_HEARTBEAT_MAX_FAILURES=3

# ==============================================================================
# TASK RECOVERY / ВОССТАНОВЛЕНИЕ ЗАДАЧ
# ==============================================================================

# Hours after which in-progress tasks are considered stale on recovery
# Часы, после которых незавершённые задачи считаются устаревшими при восстановлении
# Default: 24.0
# Valid range: 1.0-168.0 (max 1 week / максимум 1 неделя)
CHATFILTER_STALE_TASK_THRESHOLD_HOURS=24.0

# ==============================================================================
# SESSION FILE CLEANUP / ОЧИСТКА ФАЙЛОВ СЕССИЙ
# ==============================================================================

# Days after which unused session files are auto-deleted
# Дни, после которых неиспользуемые файлы сессий автоматически удаляются
# Default: None (disabled / отключено)
# Valid range: 1.0-365.0 (max 1 year / максимум 1 год)
# Uncomment to enable / Раскомментируйте для включения:
# CHATFILTER_SESSION_CLEANUP_DAYS=30.0

# ==============================================================================
# NOTES / ЗАМЕТКИ
# ==============================================================================
#
# 1. Proxy Configuration / Конфигурация прокси:
#    Proxy settings are stored in a separate JSON file, not in .env
#    Настройки прокси хранятся в отдельном JSON файле, а не в .env
#    Location / Расположение: {data_dir}/config/proxy.json
#    Configure via UI or edit the JSON file directly
#    Настройте через UI или отредактируйте JSON файл напрямую
#
# 2. File Paths / Пути к файлам:
#    All paths can be absolute or relative
#    Все пути могут быть абсолютными или относительными
#    Relative paths are resolved from the current working directory
#    Относительные пути разрешаются от текущей рабочей директории
#
# 3. Boolean Values / Булевы значения:
#    Accepted formats / Принимаемые форматы: true/false, 1/0, yes/no, on/off
#    Case insensitive / Регистр не важен
#
# 4. CLI Override Examples / Примеры переопределения через CLI:
#    chatfilter --host 0.0.0.0 --port 9000
#    chatfilter --debug --log-level DEBUG
#    chatfilter --data-dir /custom/path
#
# 5. Validation / Проверка:
#    Test your configuration before starting / Проверьте конфигурацию перед запуском:
#    chatfilter --validate
#
